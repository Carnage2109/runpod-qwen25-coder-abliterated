# RunPod env vars (set these in Console → Endpoint → Edit → Environment Variables)
# HF_TOKEN=hf_xxx          # only if the model repo becomes gated
# MODEL_NAME=huihui-ai/Qwen2.5-Coder-32B-Instruct-abliterated
# MAX_MODEL_LEN=32768
# GPU_MEMORY_UTILIZATION=0.95
# TENSOR_PARALLEL_SIZE=2   # use 2 if deploying on 2x GPU for full bf16 32B
